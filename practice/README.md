# Practice

Your answers to the lesson exercises — one folder per lesson, named like the lesson file:

```
practice/
├── go.mod
├── 03-bytes-encoding/main.go
├── 04-hash-functions/main.go
└── ...
```

This folder is **gitignored** on purpose: it is your scratch work, not course material.

```bash
cd practice
go mod tidy
go run ./04-hash-functions
```
